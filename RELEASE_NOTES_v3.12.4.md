# Release notes — v3.12.4

Updated: 2026/07/25 (UTC+8)

## 作答頁（手機，≤900px）

- 題號索引改為 React state 控制的收合區，預設收合。收合時左上角只留一顆膠囊按鈕（`題號索引 · 已作答 n / N`）。
- 展開後索引以卡片顯示，最高 46vh 可滾動；點任一題號會自動收合索引並捲回頁面頂端。
- 版面順序改為：題目資訊 → 題目 → 選項 → 上一題／下一題 → 提示 → 作答輔助。
- 上一題／下一題改為 sticky，固定在底部工具列上方 58px，滾動選項時仍可直接換題。
- 信心程度、模考／檢討標記與筆記收進「作答輔助」收合區，預設收合；標題列會顯示該題是否已加星號。
- 節奏診斷（pace coach）與「顯示節奏」按鈕在 ≤900px 完全不顯示。

桌機（>900px）與 v3.12.3 完全相同：索引固定在左側側欄、信心與標記常駐展開、節奏診斷照原邏輯、看不到任何收合按鈕。重排是用 CSS `order` 做的，DOM 順序未動。

## 歷史紀錄

- 篩選與排序收進收合區塊，預設收合，重用結果頁的 `result-collapsible` 樣式。標題列顯示「已套用 n 個條件」、目前回合數與排序方式。
- 有套用條件時，收合狀態仍以 chip 列出條件，並提供「清除全部篩選」；展開區塊底部新增統計與「回到預設」。
- 複習組卷卡片同樣改為收合，預設收合，標題列顯示已選回合數與目前抓題條件。勾選任一回合時自動展開。
- 「選取目前篩選結果」與「清除已選回合」常駐在標題列下方，收合狀態也能操作。

## 修正

- `list_my_attempts` 若回傳 `null` 陣列欄位（`selected_paper_kinds`、`selected_years`、`selected_subjects`、`selected_subsubjects`、`source_attempt_ids`），篩選與卡片渲染會丟出 `Cannot read properties of null (reading 'includes')`。改為在載入時統一正規化為空陣列；原本只有 `availablePaperKinds` 一處做 `?? []`，其餘未防護。

## Database

無 Supabase schema 變更。
