# Release notes — v3.12.6

Updated: 2026/07/25 (UTC+8)

含 v3.12.4、v3.12.5 全部內容。

## 1. 作答頁上方兩條 bar 大幅壓縮（≤900px）

實測 390×844，作答時「永遠佔住畫面頂端」的高度：**208px → 59px**，題目與選項可視高度 574px → 723px（+26%）。360×800 同樣是 208 → 59。

- 網站導覽列在作答頁改為不吸頂（`position: static`），往下滑就完全讓出空間；隱藏站名，導覽字級縮為 12px 並保持單行（原本在手機會擠成兩行，佔 92px）。導覽連結仍保有 32px 觸控高度。
- 卷名列吸頂位置改為 `top: 0`，高度鎖定 54px：卷別標籤 11px、卷名 13px 單行超出省略、計時器 19px。
- 進度條吸頂位置改用 `--exam-header-height` 計算，與卷名列之間實測 0px 縫隙。
- 只作用於作答頁（用 `body:has(.exam-shell)` 限定），其他頁面的導覽列完全不受影響；瀏覽器若不支援 `:has()` 會自動退回原本樣式，不會壞版。

## 2. 星號按鈕移到換題列

- 「上一題」左邊新增星號按鈕（☆／★ 切換，已標記為琥珀底），實測觸控區 82×49px；≤360px 自動只留星號圖示（46×49px）。
- 因為換題列在手機是 sticky 的，捲到哪都能直接標記。
- 原本在「作答輔助」收合區裡的星號按鈕已移除，避免兩個控制項並存；筆記欄位保留原位。
- 桌機同樣改為這個位置。

## Database

無 Supabase schema 變更。
