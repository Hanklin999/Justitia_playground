# Deploy status — v3.12.0

- Python corpus release gate：PASS（40 卷、2,968 題、64 題複選）。
- TypeScript／TSX transpile syntax：PASS。
- Offline TypeScript semantic check（ambient dependency stubs）：PASS。
- v3.12 migration feature gate：PASS。
- P0 pacing coach：implemented。
- P1 confidence／error diagnosis／spaced review：implemented。
- Attempt deletion with confirmation：implemented。
- npm install：目前執行環境連 registry 逾時。
- Next.js production build：請在本機執行 `npm.cmd run typecheck` 與 `npm.cmd run build`。
