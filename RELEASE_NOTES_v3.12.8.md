# Release notes — v3.12.8

Updated: 2026/07/25 (UTC+8)

含 v3.12.4／3.12.5／3.12.6 全部內容。原則：**一律用收合，不刪除、不隱藏功能**。

## 作答頁（v3.12.4–3.12.6，本包含）

- 題號索引在 ≤900px 預設收合為左上角膠囊；點題號跳題後自動收合並回頂端。
- 手機順序：題目 → 選項 → 上一題／下一題（sticky）→ 提示 → 作答輔助（信心程度、筆記，預設收合）。
- 星號按鈕移到換題列最左，觸控區 82×49px（≤360px 為 46×49px 圖示）。
- 上方兩條 bar 壓縮：390×844 實測永久佔頂高度 **208px → 59px**，題目與選項可視高度 574 → 723px（+26%）。導覽列在作答頁改為不吸頂、單行；卷名列鎖 54px、卷名單行省略。
- 底部「離開／提前交卷」改 44px 膠囊（原 52×18px），答案保存加 10 秒逾時保護，交卷鈕不會再永久停用。

## 檢討頁（新）

- **滿分題預設收合**，只留一行摘要（得分、花費時間），點「展開題目與選項」看全文。切到「滿分」頁籤時預設展開。
- **篩選頁籤改單行橫向捲動**（原本手機折 2–3 行），六個頁籤全部保留。
- **答案比較改網格**：手機 2 欄（≤340px 1 欄），電腦維持原本單行排列。
- **時間與資料來源收合**：模考日期、開始／交卷時間、時長設定、官方答案連結收進收合區。
- **右下角浮動「下一個未滿分」**：跳到下一題未拿滿分的題目，只在有未滿分題時出現，48px 觸控區。
- 題目卡片加 `scroll-margin-top`，跳題不會被吸頂列切到。

## 開始練習頁（新）

- 依年度：每個年度改為收合區塊，**最新年度預設展開**，其餘點擊展開。
- 依科目：手機上摘要列改吸底，`建立自組模考` 隨時可按。

## 今日待複習（新）

- 科目分布改收合（預設展開）、學習診斷改收合（預設收合），讓「開始複習」保持在首屏。

## 歷史紀錄（v3.12.4，本包含）

- 篩選排序與複習組卷皆為收合區塊，收合時以 chip 顯示已套用條件並可一鍵清除。

## 其他

- 新增 `components/collapsible-panel.tsx` 共用收合元件（支援 `defaultOpen`），檢討頁原本的區域元件改為引用同一份，行為完全一致。
- 首頁版本字串更新為 v3.12.8。
- README.md 改為英文技術文件，新增 README.zh-TW.md 中文使用說明，兩份都附建議截圖清單。

## Database

無 Supabase schema 變更。
